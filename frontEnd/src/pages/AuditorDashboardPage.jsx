export default function AuditorDashboardPage() {
  return (
    <section className="page-container">
      <h2>Auditor Dashboard</h2>
      <p>
        Read-only audit pages will show claim timelines, transaction hashes,
        document hashes, oracle result hashes, and settlement records.
      </p>
    </section>
  );
}