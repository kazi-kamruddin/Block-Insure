{
  "schemaVersion": 1,
  "protocolVersion": "BLOCK_INSURE_V1",
  "upgradeStrategy": "new deployments plus explicit migration; no proxy upgrade key",
  "stateExports": {
    "InsuranceManager": ["policy packages", "policies", "claims", "claim versions", "settlement records", "registry snapshots"],
    "PolicyEconomics": ["immutable term snapshots", "coverage accounts", "coverage intervals", "active exposure", "reserve configuration"],
    "ClaimAdjudicator": ["manual review snapshots", "votes", "decisions", "payout allocations", "appeal rounds", "reputation observations"],
    "EvidenceRegistry": ["encryption identities", "anchored tree heads", "trusted signer"],
    "OracleCoordinator": ["registry history", "request snapshots", "commitments", "reveals", "final outcomes"]
  },
  "eventCompatibilityRule": "Existing V1 event signatures and indexed field meanings are preserved; V2 adds new event names for incompatible payloads.",
  "migrationVerification": [
    "export block number and block hash",
    "hash canonical export",
    "commit export hash in ProtocolDeploymentRegistry",
    "reconcile policy coverage and treasury liabilities",
    "verify claim, appeal, evidence, and registry histories",
    "open V2 only after independent export reconciliation"
  ]
}
